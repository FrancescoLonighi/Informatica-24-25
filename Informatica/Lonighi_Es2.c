/*DATO UN NUMERO N POSITIVO NON NULLO MINORE DI 20 E UN NUMERO C(tra
1 e 9) STAMPARE TUTTI I MULTIPLI DI C MINORI DI N.*/
#include <stdio.h>
int main(){

int n, c, multiplo;
printf("inserisci un numero: ");
scanf("%d", &n);

printf("inserisci un valore da 1 a 9: ");
scanf("%d", &c);
if(n<20 && n>=0)
    printf("il numero inserito è valido");
else{
    printf("il numero inserito non è valido");
    }
    multiplo=c+c;
    if(n<multiplo)
        printf("il programma continua");
        if(n<multiplo)
            printf("il programma continua");
                if(n<multiplo)
                    printf("il programma continua");
                    if(n<multiplo)
                        printf("il programma continua");
                        if(n<multiplo)
                            printf("il programma continua");
                            if(n<multiplo)
                                printf("il programma continua");
                                if(n<multiplo)
                                    printf("il programma continua");
                                    if(n<multiplo)
                                        printf("il programma continua");
                                        if(n<multiplo)
                                            printf("il programma continua");

    return 0;
}